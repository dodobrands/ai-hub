# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Environment (all .env variants)
.env
.env.*
!.env.example
!.env.*.example
.env.local

# Tokens and secrets
.token
*.token
.credentials
.secrets
*.key
*.pem
*.p12
*.pfx

# Time local data (machine-specific)
integrations/time/.time-signature
integrations/time/.cache/

# Buildin local cache
integrations/buildin/.cache/

# Setup state (per-install progress markers, no secrets)
integrations/hub-meta/.setup-state

# Конфиг команды — живёт в overlay-репозитории потребителя, а не в публичном ai-hub
team-config.json
!team-config.example.json

# Node.js
node_modules/

# Playwright MCP
.playwright-mcp/

# Large external repositories
Temp/

# Shadow-индекс Buildin — локальный кеш структуры вики, у каждого свой
integrations/buildin/shadow-index.json
